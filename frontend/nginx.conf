user  nginx;
worker_processes  auto;

events {
  worker_connections  1024;
}

http {
  include       /etc/nginx/mime.types;
  default_type  application/octet-stream;
  sendfile      on;
  keepalive_timeout  65;
  resolver 172.20.0.10 valid=30s;
  resolver_timeout 5s;

  log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
  access_log  /var/log/nginx/access.log  main;
  error_log   /var/log/nginx/error.log warn;

  server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location = /healthz {
      add_header Content-Type text/plain;
      return 200 'ok';
    }

    location / {
      try_files $uri /index.html;
      add_header Cache-Control "public, max-age=300";
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
      try_files $uri =404;
      add_header Cache-Control "public, max-age=31536000, immutable";
    }

    location = /api/catalog {
      proxy_pass http://catalog.bookstore.svc.cluster.local/catalog;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location /api/catalog/ {
      proxy_pass http://catalog.bookstore.svc.cluster.local/catalog/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/catalog/healthz {
      proxy_pass http://catalog.bookstore.svc.cluster.local/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/cart {
      proxy_pass http://cart.bookstore.svc.cluster.local/cart;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location /api/cart/ {
      proxy_pass http://cart.bookstore.svc.cluster.local/cart/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/cart/healthz {
      proxy_pass http://cart.bookstore.svc.cluster.local/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/orders {
      proxy_pass http://order.bookstore.svc.cluster.local/orders;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location /api/orders/ {
      proxy_pass http://order.bookstore.svc.cluster.local/orders/;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/orders/healthz {
      proxy_pass http://order.bookstore.svc.cluster.local/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/inventory/apply {
      proxy_pass http://inventory-service.bookstore.svc.cluster.local:8080/inventory/apply;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location /api/inventory/ {
  rewrite ^/api/inventory/(.*)$ /inventory/$1 break;
  proxy_pass http://inventory-service.bookstore.svc.cluster.local:8080;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/inventory/healthz {
  proxy_pass http://inventory-service.bookstore.svc.cluster.local:8080/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/payments {
  proxy_pass http://payment-service.bookstore.svc.cluster.local:8080/payments;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/payment/healthz {
  proxy_pass http://payment-service.bookstore.svc.cluster.local:8080/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/notification/notify {
  proxy_pass http://notification-service.bookstore.svc.cluster.local:8080/notify;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/notification/healthz {
  proxy_pass http://notification-service.bookstore.svc.cluster.local:8080/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }

    location = /api/recommendations {
      proxy_pass http://recommendation-service.bookstore.svc.cluster.local:8080/recommendations;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location /api/recommendations/ {
      rewrite ^/api/recommendations/(.*)$ /recommendations/$1 break;
      proxy_pass http://recommendation-service.bookstore.svc.cluster.local:8080;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 60s;
    }

    location = /api/recommendations/healthz {
      proxy_pass http://recommendation-service.bookstore.svc.cluster.local:8080/healthz;
      proxy_set_header Host $host;
      proxy_set_header X-Forwarded-For $remote_addr;
      proxy_set_header X-Request-Id $request_id;
      proxy_read_timeout 30s;
    }
  }
}

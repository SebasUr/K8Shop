apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  labels:
    app: cart-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
    spec:
      containers:
      - name: cart
        image: YOUR_REGISTRY/cart-service:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: HOST
          value: "0.0.0.0"
        - name: PORT
          value: "8080"
        - name: CART_USE_REDIS
          value: "1"
        - name: CART_REDIS_URL
          value: "redis://redis:6379/0"
        - name: CART_PUBLISH_ENABLED
          value: "0"
        - name: RABBIT_URL
          value: "amqp://guest:guest@rabbitmq:5672/"
        - name: ORDERS_EXCHANGE
          value: "orders"
        ports:
        - containerPort: 8080
          name: http
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  labels:
    app: cart-service
spec:
  selector:
    app: cart-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP

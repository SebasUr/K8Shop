apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: inventory-service
  template:
    metadata:
      labels:
        app: inventory-service
    spec:
      containers:
      - name: inventory-service
        image: your-dockerhub-username/inventory-service:latest
        env:
        - name: RABBITMQ_HOST
          value: "rabbitmq"
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: rds-credentials-inventory
              key: database_url
        - name: DB_SCHEMA
          valueFrom:
            secretKeyRef:
              name: rds-credentials-inventory
              key: schema
---
apiVersion: v1
kind: Service
metadata:
  name: inventory-service
spec:
  selector:
    app: inventory-service
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP

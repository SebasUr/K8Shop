# Local configuration for recommendation-service
HOST=127.0.0.1
PORT=8080

# Recommendation behavior
# Default strategy: popular | related
REC_STRATEGY=popular
# Default max items
REC_LIMIT=5

# Database (RDS Postgres) — service-specific user/schema
DB_SCHEMA=recommendation
DATABASE_URL=postgres://recommendation_user:password@your-rds-endpoint:5432/bookstore?sslmode=require&options=-c%20search_path%3Drecommendation
